AUTHENTIK_SECRET_KEY={{ authentik_secret_key }}
AUTHENTIK_LOG_LEVEL={{ authentik_log_level }}

AUTHENTIK_DB_HOST={{ authentik_db_host }}
AUTHENTIK_DB_PORT={{ authentik_db_port }}
AUTHENTIK_DB_NAME={{ authentik_db_name }}
AUTHENTIK_DB_USER={{ authentik_db_user }}
AUTHENTIK_DB_PASSWORD={{ authentik_db_password }}

AUTHENTIK_REDIS_HOST={{ authentik_redis_host }}
AUTHENTIK_REDIS_PORT={{ authentik_redis_port }}
AUTHENTIK_REDIS_PASSWORD={{ authentik_redis_password }}

AUTHENTIK_EMAIL_HOST={{ authentik_email_host }}
AUTHENTIK_EMAIL_PORT={{ authentik_email_port }}
AUTHENTIK_EMAIL_USERNAME={{ authentik_email_username }}
AUTHENTIK_EMAIL_PASSWORD={{ authentik_email_password }}
AUTHENTIK_EMAIL_USE_TLS={{ authentik_email_use_tls | lower }}
AUTHENTIK_EMAIL_FROM={{ authentik_email_from }}

AUTHENTIK_LISTEN_PORT={{ authentik_listen_port }}
AUTHENTIK_TRUSTED_PROXY_CIDRS={{ authentik_trusted_proxy_cidrs | join(',') }}

AUTHENTIK_WORKER_THREADS={{ authentik_worker_threads }}