---
- name: Restart PHP-FPM
  ansible.builtin.systemd:
    name: "php{{ nextcloud_php_version }}-fpm"
    state: restarted
    daemon_reload: true

- name: Restart nginx
  ansible.builtin.systemd:
    name: nginx
    state: restarted
