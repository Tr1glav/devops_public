---
apiVersion: v1
kind: Service
metadata:
  name: grafana-external
  namespace: monitoring
spec:
  type: LoadBalancer
  ports:
    - name: https
      port: 80
      targetPort: grafana
      protocol: TCP
  selector:
    app.kubernetes.io/name: grafana
